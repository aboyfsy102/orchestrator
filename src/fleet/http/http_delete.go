package main

import (
	"context"

	"github.com/aws/aws-lambda-go/events"
)

func handleDelete(ctx context.Context, queryParams map[string]string) (events.ALBTargetGroupResponse, error) {
	panic("unimplemented")
}
